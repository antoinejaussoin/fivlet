export type Translations = {
	alerts: {
		'lost-already': string;
		lost: string;
		won: string;
		'not-a-word': string;
		'won-already': string;
	};
	home: { title: string };
	keyboard: { first: string; second: string; third: string };
};
